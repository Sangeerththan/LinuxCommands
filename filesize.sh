#disk space of partitions
df
df -m # file size in mb

#size of particular file 
du filename

#list files with file sizes
ls -lah

# compress and uncompress files
tar -zxvf compressed.tar.gz location
zip
unzip